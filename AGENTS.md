# Personal Pi Agent Workspace Guidelines

## Working style

- 先检查现有文件、项目状态和相关上下文，再提出修改方案。
- 采用小步、可验证的变更，不进行未经确认的整体迁移。
- 保留用户已有修改，不覆盖无关文件。
- 修改后运行与变更范围匹配的验证，并记录结果。

## Git workflow

- 开始开发前先同步远端 `main`，再从最新基线创建 `codex/*` 功能分支；不直接在 `main` 上开发。
- 完成修改和本地验证后提交功能分支并 push，随后创建 Pull Request，不绕过 PR 直接更新远端 `main`。
- 创建 PR 后检查完整 diff、自动检查和审查意见；发现问题时继续在同一分支修复并重新验证。
- 只有在审查和检查通过后才合并 PR；合并后切回本地 `main`，同步 `origin/main` 并确认工作区干净。
- 用户已授权本仓库在上述流程内执行功能分支 push、创建 PR 和合并；GitHub 登录、权限变化、其他仓库写入及流程外的外部操作仍需确认。

## Agent boundaries

- 将个人知识视为可检索的外部知识源，而不是默认全部放入上下文。
- 对知识库写入保留来源、时间、项目和不确定性信息。
- 区分原文事实、模型总结、模型推断和用户判断。
- 执行删除、安装、上传、登录和其他外部写操作前请求确认；本仓库 Git push、PR 与合并按上述已授权流程执行。

## Project direction

本工作区用于开发个人 Pi Agent，包括知识库、工作流、任务执行层和自定义界面。
