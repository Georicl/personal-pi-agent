[project]
name = "personal-pi-knowledge-core"
version = "0.1.0"
description = "Local, provenance-preserving knowledge storage and retrieval core for Personal Pi"
requires-python = ">=3.11,<3.15"
dependencies = [
  "pypdf>=6,<7",
  "pyyaml>=6,<7",
]

[tool.uv]
package = false
